<script type="text/javascript">var productName = "{{product.title | escape}}", productId = "{{product.id}}", productImageUrl = "{{ product.images[0] | product_img_url: 'medium' }}"; var productUrl = "{{product.url}}", productPrice = "{{product.price  | money_without_currency}}"; var productStock = "{{product.available}}", productCollection = "{{product.collections[0]}}"; var collectionTitle = "{{collection.title}}", collectionId = "{{collection.id}}",collectionUrl = "{{collection.url}}"; var customerName = "{{customer.name | default: 'anonymous'}}", userId = "{{customer.id | default: ''}}",userMail = "{{customer.email}}"; var pageName = "{{page.title}}",orderIdNew="{{order.order_number}}"; var pageType = "{{request.page_type}}"; var productStockStatus =  '0'; if  (productStock == 'false') { productStockStatus = '1'; }</script>